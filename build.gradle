allprojects {
    group = 'bhw.bihrys'
    version = '1.0.0'
}

tasks.register('buildAll') {
    group = 'build'
    description = 'Build every supported Minecraft version.'
    dependsOn(
            ':common:build',
            ':fabric-1.21.1:build',
            ':fabric-1.21.11:build',
            ':fabric-26.1:build',
            ':fabric-26.1.1:build',
            ':fabric-26.1.2:build',
            ':fabric-26.2:build'
    )
}
