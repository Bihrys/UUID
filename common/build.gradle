plugins {
    id 'java-library'
}

java {
    // CI runs Gradle on Java 25 while emitting Java 21-compatible bytecode.
    toolchain.languageVersion = JavaLanguageVersion.of(25)
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
    options.release = 21
}
